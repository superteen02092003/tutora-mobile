<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <!-- Ghi âm buổi học: bản ghi phải chạy tiếp khi gia sư khoá màn hình hoặc
         mở app khác tra bài, nên cần foreground service. Từ Android 14 phải
         khai báo thêm loại "microphone" — thiếu là service không start được.
         POST_NOTIFICATIONS (Android 13+) để hiện thông báo "đang ghi", vừa là
         yêu cầu của foreground service vừa là chỉ báo bắt buộc theo chính sách
         cửa hàng: người trong phòng phải biết máy đang ghi. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <!-- flutter_foreground_task tự thêm quyền này cho autoRunOnBoot; app không
         tự ghi âm sau khi khởi động máy nên gỡ khỏi manifest đã gộp (Google đối
         chiếu danh sách quyền với phần khai báo Data safety). -->
    <uses-permission
        android:name="android.permission.RECEIVE_BOOT_COMPLETED"
        tools:node="remove"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- HTTP thường (cleartext) chỉ bật ở bản debug: xem src/debug/AndroidManifest.xml. -->
    <application
        android:usesCleartextTraffic="false"
        android:label="Tutora"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- flutter_foreground_task: service giữ cho bản ghi chạy tiếp khi app
             xuống nền. Loại "microphone" phải khớp với quyền
             FOREGROUND_SERVICE_MICROPHONE khai ở trên; sai loại thì Android 14+
             ném SecurityException ngay lúc startService. -->
        <service
            android:name="com.pravera.flutter_foreground_task.service.ForegroundService"
            android:foregroundServiceType="microphone"
            android:exported="false" />

        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_stat_name" />
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
